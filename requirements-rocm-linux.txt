# Fizgig — AMD ROCm extras (Linux only — HIGHLY EXPERIMENTAL).
# PyTorch + ROCm: install_fizgig_rocm_linux.sh (stable 7.14 from repo.amd.com by default).
# bitsandbytes ships libbitsandbytes_rocm714.so — run_fizgig_rocm.sh sets BNB_ROCM_VERSION=714.
# Pair with requirements-global.txt.

# 0.50+ bundles ROCm 7.14 libs (libbitsandbytes_rocm714.so). Must match torch's ROCm SDK.
bitsandbytes>=0.50.0

# VRAM status bar: optional system package — amd-smi for accurate used/total VRAM.
#   sudo apt install amdrocm-amdsmi    # Debian/Ubuntu
#   sudo dnf install amdrocm-amdsmi    # RHEL/Fedora
# amd-smi lives under /opt/rocm/core-*/bin (run_fizgig.sh adds it to PATH). Legacy rocm-smi also works.
# Do NOT pip install amdsmi — the PyPI wheel is stale; upstream is rocm-systems/projects/amdsmi:
# https://rocm.docs.amd.com/projects/amdsmi/en/latest/install/install.html
#
# rocSHMEM (bundled with PyTorch ROCm) dlopens libnuma.so (unversioned). install_fizgig_rocm_linux.sh
# installs libnuma-dev (Debian/Ubuntu) or numactl-devel (Fedora/RHEL) when the symlink is missing.
# Triton/torch.compile needs Python.h — install_fizgig_rocm_linux.sh installs pythonX.Y-dev for the
# system interpreter used to create the venv (not conda); deactivate conda before installing.
