# Fizgig — AMD ROCm PyTorch wheels (Linux).
# Pair with requirements-global.txt. Windows ROCm uses install_fizgig_rocm.bat instead.

--extra-index-url https://download.pytorch.org/whl/rocm7.1
torch==2.10.0+rocm7.1; sys_platform == "linux"
torchvision==0.25.0+rocm7.1; sys_platform == "linux"

# bitsandbytes ROCm build (Linux). Omit on Windows — install_fizgig_rocm.bat uses the 0xDELUXA wheel.
bitsandbytes==0.48.2; sys_platform == "linux"

# VRAM status bar (Linux AMD): NOT a pip package — install AMD SMI from ROCm Core / system packages:
#   sudo apt install amdrocm-amdsmi    # Debian/Ubuntu
#   sudo dnf install amdrocm-amdsmi    # RHEL/Fedora
# amd-smi lives under /opt/rocm/core-*/bin (run_fizgig.sh adds it to PATH). Legacy rocm-smi also works.
# Do NOT pip install amdsmi — the PyPI wheel is stale; upstream is rocm-systems/projects/amdsmi:
# https://rocm.docs.amd.com/projects/amdsmi/en/latest/install/install.html
